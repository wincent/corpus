{
  "name": "corpus",
  "version": "0.0.9000-pre",
  "description": "A note-management application for macOS",
  "main": "dist/main/index.js",
  "repository": "https://github.com/wincent/corpus.git",
  "author": "Greg Hurrell <greg@hurrell.net>",
  "license": "MIT",
  "private": false,
  "dependencies": {
    "@wincent/clamp": "0.3.0",
    "@wincent/frozen-set": "0.0.1",
    "@wincent/throttle": "0.0.2",
    "electron": "6.0.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "rxjs": "6.5.2"
  },
  "devDependencies": {
    "@types/jest": "24.0.16",
    "@types/react": "16.8.24",
    "@types/react-dom": "16.8.5",
    "@typescript-eslint/eslint-plugin": "1.13.0",
    "@typescript-eslint/parser": "1.13.0",
    "eslint": "6.1.0",
    "eslint-config-prettier": "6.0.0",
    "eslint-plugin-notice": "0.8.8",
    "eslint-plugin-react": "7.14.3",
    "eslint-plugin-react-hooks": "1.6.1",
    "jest": "24.8.0",
    "prettier": "*",
    "ts-jest": "24.0.2",
    "typescript": "3.5.3",
    "yarn-deduplicate": "1.1.1"
  },
  "scripts": {
    "build": "tsc",
    "ci": "yarn format:check && yarn lint && yarn typecheck && yarn test",
    "clean": "rm -r dist/*",
    "deduplicate": "yarn-deduplicate yarn.lock",
    "fix": "yarn format && yarn lint:fix && yarn deduplicate",
    "format": "prettier --write -- '*.{js,json,md}' 'src/**/*.{html,ts,tsx}'",
    "format:check": "prettier --list-different -- '*.{js,json,md}' 'src/**/*.{html,ts,tsx}'",
    "lint": "eslint '.*.js' 'src/**/*.{ts,tsx}'",
    "lint:fix": "eslint --fix '.*.js' 'src/**/*.{ts,tsx}'",
    "start": "electron .",
    "test": "jest",
    "typecheck": "tsc --noEmit",
    "watch": "tsc --watch"
  }
}
